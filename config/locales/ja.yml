ja:
  activerecord:
    errors:
      messages:
        record_invalid: "バリデーションに失敗したぜええ： %{errors}"
        restrict_dependent_destroy:
          has_one: "%{record}が存在しているので削除できないぜええ"
          has_many: "%{record}が存在しているので削除できないぜええ"
    models:
      task: タスク
    attributes:
      task:
        id: ID
        name: 名称
        desctription: 詳しい説明
        created_at: 登録日時
        updated_at: 更新日時